package com.example.studdybuddy.session;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;

public class SessionProgressReceiver extends BroadcastReceiver {

    @Override
    public void onReceive(Context ctx, Intent intent) {
        /*
              todo: call this after 30 seconds away from the application
                    to mark the session as paused

                    probably gonna have to build it from scratch but thats ok :-)
         */
    }

}
